spring:
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':  # Applies to all paths
            allowedOrigins: "*"
            allowedMethods: GET, POST, PUT, DELETE, OPTIONS
            allowedHeaders: "*"
      routes:
        - id: auth_route
          uri: lb://authservice
          predicates:
            - Path=/api/au/**
        - id: booking_route
          uri: lb://bookingservice
          predicates:
            - Path=/api/book/**   